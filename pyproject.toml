[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "valheim-codex"
version = "0.1.0"
description = "Local, vision-first Valheim player bridge and persistent Codex supervisor"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
